# Archived pre-baseline migrations (001–017)

These files are **not** loaded by Alembic. They are kept only for historical reference.

Active migration path:

    alembic/versions/001_waza_baseline.py

Do not move these files back into `alembic/versions/` without a deliberate migration strategy.
